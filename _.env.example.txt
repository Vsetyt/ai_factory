# =============================================================================
# AI Factory — .env.example
# Скопируй в .env и заполни нужные ключи.
# Достаточно одного провайдера, остальные — резервные.
# =============================================================================

# ── OpenRouter (основной) ─────────────────────────────────────────────────────
# Регистрация: https://openrouter.ai — бесплатно, без карты
OPENROUTER_API_KEY=sk-or-v1-ЗАМЕНИТЕ

# ── GitHub Models (бесплатный GPT-4.1) ───────────────────────────────────────
# Создать токен: github.com → Settings → Developer settings →
# Personal access tokens → Fine-grained tokens
# Права: Account permissions → Models → Read-only
GITHUB_TOKEN=github_pat_ЗАМЕНИТЕ

# ── AIHubMix (резерв) ─────────────────────────────────────────────────────────
# Регистрация: https://aihubmix.com/token
AIHUBMIX_API_KEY=sk-ЗАМЕНИТЕ

# ── Groq (резерв, очень быстрый) ─────────────────────────────────────────────
# Регистрация: https://console.groq.com/keys
GROQ_API_KEY=gsk_ЗАМЕНИТЕ

# ── Telegram ──────────────────────────────────────────────────────────────────
# Токен от @BotFather, ваш ID от @userinfobot
TELEGRAM_BOT_TOKEN=ЗАМЕНИТЕ_НА_ТОКЕН_ОТ_BOTFATHER
TELEGRAM_ADMIN_ID=ЗАМЕНИТЕ_НА_ВАШ_ID

# ── Пути (подставляются автоматически через install.sh) ──────────────────────
WORKSPACE_PATH=БУДЕТ_ПОДСТАВЛЕНО
LOGS_PATH=БУДЕТ_ПОДСТАВЛЕНО

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379

# ── Лимиты ────────────────────────────────────────────────────────────────────
MAX_ITERATIONS=10
MAX_EXECUTION_TIME=900
MAX_CONCURRENT_TASKS=2
MEMORY_LIMIT=512m
CPU_QUOTA=100000
